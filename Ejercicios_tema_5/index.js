const altura_cm = 163;
const altura_m = 1.63;
const peso_kg = 70.1;

const altura_redondeo = Math.ceil(altura_m);
const peso_redondeo = Math.floor(peso_kg);

const numMaxIguales = Number.MAX_VALUE + 1 === Number.MAX_VALUE;
